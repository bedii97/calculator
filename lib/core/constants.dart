import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.deepPurple;
  static final buttonBackground = Colors.grey.shade800;
  static const error = Colors.redAccent;
}

class AppTextStyles {
  static const buttonText = TextStyle(fontSize: 24);
  static const displayText = TextStyle(fontSize: 32, color: Colors.white70);
  static const resultText = TextStyle(
    fontSize: 48,
    fontWeight: FontWeight.bold,
    color: Colors.white,
  );
}
